/* ./handlers/database_handler.js */
const express = require('express');
const router = express.Router();

// I don't think this is needed, but keeping until I implement the rest of the database for the other tools.
router.post("/", function (req, res) {
});

module.exports = router;
